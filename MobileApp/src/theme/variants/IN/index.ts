import * as Colors from './colors';
import {Theme} from 'types';

const baseIndia: Theme = {
  colors: {
    primary: Colors.PRIMARY,
    action: Colors.ACTION,
    error: Colors.ERROR,
  },
};

export default baseIndia;
